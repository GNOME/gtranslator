<?xml version="1.0"?>
<!-- The backend informations -->
<backend name="text" _description="Plain text files backend">

	<!-- Backend module name: -->
	<module>text.so</module>
	
	<!-- Backend mime types: -->
	<type>text/*</type>

	<!-- Is it compilable ? -->
	<compilable>no</compilable>
</backend>
